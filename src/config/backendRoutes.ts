export const BACKEND_ROUTES = {
  users: "/users",
  login: "/auth/login",
  register: "/auth/register",
  streams: "/streams",
  transcripts: "/transcripts",
  payments: "/payments",
  settings: "/settings",
} as const;
